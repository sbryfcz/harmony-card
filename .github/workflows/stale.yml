name: Mark stale issues and pull requests

on:
  schedule:
  - cron: "30 1 * * *"

jobs:
  stale:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/stale@v3
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        stale-issue-message: 'This issue is being marked as stale due to lack of activity'
        stale-pr-message: 'This pr is being marked as stale due to lack of activity'
        stale-issue-label: 'no-issue-activity'
        stale-pr-label: 'no-pr-activity'
